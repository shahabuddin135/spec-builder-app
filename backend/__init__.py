"""Marketing Strategy Spec Generator — backend package.

Run from the repo root:  uvicorn backend.main:app --reload
Packaging the backend (rather than running flat from backend/) keeps the
`backend/agents/` directory from shadowing the OpenAI Agents SDK's top-level
`agents` package.
"""
