```slc
@block TASK 1.2_config
priority: critical
intent: "Env-loaded settings + budgets + constants (config.py)"
scope: phase-1
depends_on: [1.1_init_project]
estimate: 8

content:
  - pydantic-settings BaseSettings reading canonical env vars:
      DATABASE_URL, LLM_API_KEY, GROQ_API_KEY, OPENAI_API_KEY, ALLOWED_ORIGIN, RATE_LIMIT.
  - Constants: MAX_AGENT_INPUT_TOKENS=800, MAX_UPLOAD_BYTES=256*1024, MAX_OUTPUT_TOKENS=256,
      MAX_REFINE_ITERATIONS=3, ALLOWED_EXTENSIONS={".txt",".md"}, ALLOWED_CONTENT_TYPES.
  - Model routing config: GROQ model id (primary), OPENAI model id (fallback).
  - has_llm() helper: true only if a usable key is present (drives fallback path).

acceptance_criteria:
  - Settings load from env; missing optional LLM keys do not crash (degrade to deterministic).
  - No secret defaults baked into source.
@end
```
