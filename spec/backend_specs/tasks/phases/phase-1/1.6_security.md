```slc
@block TASK 1.6_security
priority: critical
intent: "Upload validation, filename sanitize, size guard, rate limit (security.py)"
scope: phase-1
depends_on: [SECURITY.md, 1.2_config]
estimate: 14

content:
  - validate_upload(file): allowlist extension (.txt/.md) AND content-type; enforce
      MAX_UPLOAD_BYTES (413); reject null bytes / non-UTF-8 decode (422); strip control chars.
  - safe_name(): server-generated UUID name — never trust user filename (path-traversal guard).
  - sanitize_download_name(name): whitelist [A-Za-z0-9._-], strip path separators, for
      Content-Disposition.
  - rate limiter (slowapi) configured from RATE_LIMIT; applied to /analyze, /generate-specs.

acceptance_criteria:
  - .pdf / wrong content-type -> 415; >256KB -> 413; binary/null-byte -> 422.
  - Stored filename is a UUID; no user path reaches disk.
  - SECURITY.md P0 upload rules all satisfied.
@end
```
