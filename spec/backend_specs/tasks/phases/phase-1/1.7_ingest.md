```slc
@block TASK 1.7_ingest
priority: critical
intent: "Validated text -> deterministic FeatureCard (NO LLM) (ingest.py)"
scope: phase-1
depends_on: [ARCH.data_model, 1.5_schemas]
estimate: 14

content:
  - build_feature_card(text) -> FeatureCard using deterministic heuristics ONLY:
      keyword/frequency signals -> f{price_sens,loyalty,recency,novelty,engage,is_new} in [0,1];
      derive persona label and up to 4 tags. No randomness; same input -> same output.
  - extract_brand_card_defaults(text) -> BrandCard{tone, palette[], restricted_keywords[]}
      deterministic baseline (used when no LLM; LLM Analyst refines later).
  - This is the fallback that keeps the whole product working with the LLM key removed.

acceptance_criteria:
  - Pure function, no network/LLM. Deterministic across runs.
  - Output validates against schemas.FeatureCard (~40 tokens compact).
@end
```
