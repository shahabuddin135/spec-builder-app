```slc
@block TASK 1.1_init_project
priority: critical
intent: "Initialize backend/ Python project and pinned dependency list"
scope: phase-1
depends_on: none
estimate: 8

content:
  - Create backend/ with the §3 file layout (empty single-purpose modules + agents/ + seed/).
  - Create requirements.txt with PINNED versions (verify latest via Context7 before pinning):
      fastapi, uvicorn[standard], pydantic, pydantic-settings, sqlalchemy[asyncio], asyncpg,
      python-multipart, slowapi, litellm, openai-agents, httpx.
  - Create backend/.env.example with canonical env var names ONLY (no real values).
  - Ensure .env and .slc_secrets are git-ignored.

acceptance_criteria:
  - backend/ tree matches requirements.md §3.
  - `pip install -r requirements.txt` resolves in a clean venv (python 3.11).
  - No secrets committed; .env.example uses placeholders.
@end
```
