```slc
@block TASK 1.5_schemas
priority: high
intent: "Pydantic request/response + agent I/O schemas (schemas.py)"
scope: phase-1
depends_on: [CONTRACT.http_api, CONTRACT.agent_io]
estimate: 10

content:
  - Request models: AnalyzeReq{document_id}, RefineReq{analysis_id, feedback},
      FeedbackReq{strategy_id, action: Literal["approve","reject"]},
      GenerateSpecsReq{analysis_id, approved_ids:list[str]}.
  - Response models: UploadResp{document_id}, AnalyzeResp{analysis_id},
      RefineResp{analysis_id, iteration}, SpecResp{spec_id, files:list[SpecFile]}.
  - Agent I/O: FeatureCard, BrandCard, Suggestion(strategy_id enum), AnalystOut, StrategistOut,
      SpecFile{name, mime, content} — mirror CONTRACT.agent_io exactly.
  - Strategy enum sourced from the live library ids (single source: strategies.py).

acceptance_criteria:
  - Schemas validate the §10 payloads; strategy_id is enum-constrained.
  - SpecFile shape matches specs.files in ARCH.data_model.
@end
```
