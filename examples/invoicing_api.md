# Freelance Invoicing API

A REST API for freelancers to manage clients and send invoices.

- Create and manage clients
- Create invoices with line items
- Mark invoices as paid
- Generate a PDF for an invoice

The API is consumed by a separate mobile app, so there is no web UI in this project.
Use FastAPI and PostgreSQL.
